{
  "name": "eventy3",
  "version": "1.0.0",
  "private": true,
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "rm -rf build && mkdir -p build && cp -R assets css js index.html build/ && npm ci --legacy-peer-deps --prefix iphone-3d-react && npm run build --prefix iphone-3d-react && cp -R iphone-3d-react/build build/iphone-embed",
    "clean": "rm -rf iphone-embed iphone-3d-react/build iphone-3d-react/node_modules"
  }
}

